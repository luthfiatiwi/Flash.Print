export default function Footer({ className }) {
    return (
        <footer className={`bg-red-800 text-white text-center py-6 font-serif ${className}`}>
            Copyright 2025
        </footer>
    )
}